/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var live_connection_utils_exports = {};
__export(live_connection_utils_exports, {
  LiveResponseAggregator: () => LiveResponseAggregator
});
module.exports = __toCommonJS(live_connection_utils_exports);
var import_model_name = require("./model_name.js");
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
class LiveResponseAggregator {
  constructor(modelVersion) {
    this.modelVersion = modelVersion;
    this.text = "";
    this.isThought = false;
    this.toolCallParts = [];
    this.pendingGroundingMetadata = void 0;
    this.inputTranscriptionText = "";
    this.outputTranscriptionText = "";
  }
  *processMessage(message) {
    if (message.usageMetadata) {
      yield {
        usageMetadata: message.usageMetadata,
        ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
      };
    }
    if (message.serverContent) {
      const serverContent = message.serverContent;
      const content = serverContent.modelTurn;
      if (serverContent.groundingMetadata) {
        this.pendingGroundingMetadata = serverContent.groundingMetadata;
      }
      if (!(content && content.parts) && serverContent.groundingMetadata && !serverContent.turnComplete) {
        yield {
          groundingMetadata: serverContent.groundingMetadata,
          ...serverContent.interrupted !== void 0 ? { interrupted: serverContent.interrupted } : {},
          ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
        };
      }
      if (content && content.parts) {
        const llmResponse = {
          content,
          ...serverContent.interrupted !== void 0 ? { interrupted: serverContent.interrupted } : {},
          ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
        };
        if (!serverContent.turnComplete && serverContent.groundingMetadata) {
          llmResponse.groundingMetadata = serverContent.groundingMetadata;
        }
        const hasInlineData = content.parts.some((p) => p.inlineData);
        for (const part of content.parts) {
          if (part.text) {
            const currentIsThought = !!part.thought;
            if (this.text && currentIsThought !== this.isThought) {
              yield this.buildFullTextResponse(this.text, this.isThought);
              this.text = "";
              this.isThought = false;
            }
            this.text += part.text;
            this.isThought = currentIsThought;
            llmResponse.partial = true;
          }
        }
        if (this.text && !content.parts.some((p) => p.text) && !hasInlineData) {
          yield this.buildFullTextResponse(this.text, this.isThought);
          this.text = "";
          this.isThought = false;
        }
        yield llmResponse;
      }
      if (serverContent.inputTranscription) {
        if (serverContent.inputTranscription.text) {
          this.inputTranscriptionText += serverContent.inputTranscription.text;
          yield {
            inputTranscription: {
              text: serverContent.inputTranscription.text,
              finished: false
            },
            partial: true,
            ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
          };
        }
        if (serverContent.inputTranscription.finished) {
          yield {
            inputTranscription: {
              text: this.inputTranscriptionText,
              finished: true
            },
            partial: false,
            ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
          };
          this.inputTranscriptionText = "";
        }
      }
      if (serverContent.outputTranscription) {
        if (serverContent.outputTranscription.text) {
          this.outputTranscriptionText += serverContent.outputTranscription.text;
          yield {
            outputTranscription: {
              text: serverContent.outputTranscription.text,
              finished: false
            },
            partial: true,
            ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
          };
        }
        if (serverContent.outputTranscription.finished) {
          yield {
            outputTranscription: {
              text: this.outputTranscriptionText,
              finished: true
            },
            partial: false,
            ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
          };
          this.outputTranscriptionText = "";
        }
      }
      if (serverContent.interrupted || serverContent.turnComplete || serverContent.generationComplete) {
        if (this.inputTranscriptionText) {
          yield {
            inputTranscription: {
              text: this.inputTranscriptionText,
              finished: true
            },
            partial: false,
            ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
          };
          this.inputTranscriptionText = "";
        }
        if (this.outputTranscriptionText) {
          yield {
            outputTranscription: {
              text: this.outputTranscriptionText,
              finished: true
            },
            partial: false,
            ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
          };
          this.outputTranscriptionText = "";
        }
      }
      if (serverContent.turnComplete) {
        let gMetadataToYield = this.pendingGroundingMetadata;
        if (this.text) {
          yield this.buildFullTextResponse(
            this.text,
            this.isThought,
            gMetadataToYield
          );
          this.text = "";
          this.isThought = false;
          gMetadataToYield = void 0;
        }
        if (this.toolCallParts.length > 0) {
          yield {
            content: { role: "model", parts: this.toolCallParts },
            ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
          };
          this.toolCallParts = [];
        }
        const finalResponse = {
          turnComplete: true,
          ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
        };
        if (serverContent.interrupted !== void 0) {
          finalResponse.interrupted = serverContent.interrupted;
        }
        const finalGrounding = serverContent.groundingMetadata || gMetadataToYield;
        if (finalGrounding !== void 0 && finalGrounding !== null) {
          finalResponse.groundingMetadata = finalGrounding;
        }
        yield finalResponse;
      }
      if (serverContent.interrupted) {
        if (this.text) {
          yield this.buildFullTextResponse(this.text, this.isThought);
          this.text = "";
          this.isThought = false;
        } else {
          yield {
            interrupted: serverContent.interrupted,
            ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
          };
        }
      }
    }
    if (message.toolCall) {
      if (this.text) {
        yield this.buildFullTextResponse(this.text, this.isThought);
        this.text = "";
        this.isThought = false;
      }
      if (message.toolCall.functionCalls) {
        this.toolCallParts.push(
          ...message.toolCall.functionCalls.map((fc) => ({
            functionCall: fc
          }))
        );
      }
      const isGemini3x = (0, import_model_name.isGemini3xFlashLive)(this.modelVersion);
      if (isGemini3x && this.toolCallParts.length > 0) {
        yield {
          content: { role: "model", parts: this.toolCallParts },
          ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
        };
        this.toolCallParts = [];
      }
    }
    if (message.sessionResumptionUpdate) {
      yield {
        liveSessionResumptionUpdate: message.sessionResumptionUpdate,
        ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
      };
    }
    if (message.goAway) {
      yield {
        goAway: message.goAway,
        ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
      };
    }
  }
  /**
   * Flushes any remaining aggregated components when the connection is closed.
   */
  *close() {
    if (this.toolCallParts.length > 0) {
      yield {
        content: { role: "model", parts: this.toolCallParts },
        ...this.modelVersion ? { modelVersion: this.modelVersion } : {}
      };
      this.toolCallParts = [];
    }
  }
  buildFullTextResponse(text, isThought, groundingMetadata) {
    const part = { text };
    if (isThought) {
      part.thought = true;
    }
    const response = {
      content: {
        role: "model",
        parts: [part]
      },
      partial: false
    };
    if (groundingMetadata !== void 0 && groundingMetadata !== null) {
      response.groundingMetadata = groundingMetadata;
    }
    if (this.modelVersion) {
      response.modelVersion = this.modelVersion;
    }
    return response;
  }
}
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  LiveResponseAggregator
});
