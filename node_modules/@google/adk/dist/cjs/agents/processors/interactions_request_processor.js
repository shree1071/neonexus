/**
  * @license
  * Copyright 2026 Google LLC
  * SPDX-License-Identifier: Apache-2.0
  */

"use strict";
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __getOwnPropNames = Object.getOwnPropertyNames;
var __hasOwnProp = Object.prototype.hasOwnProperty;
var __export = (target, all) => {
  for (var name in all)
    __defProp(target, name, { get: all[name], enumerable: true });
};
var __copyProps = (to, from, except, desc) => {
  if (from && typeof from === "object" || typeof from === "function") {
    for (let key of __getOwnPropNames(from))
      if (!__hasOwnProp.call(to, key) && key !== except)
        __defProp(to, key, { get: () => from[key], enumerable: !(desc = __getOwnPropDesc(from, key)) || desc.enumerable });
  }
  return to;
};
var __toCommonJS = (mod) => __copyProps(__defProp({}, "__esModule", { value: true }), mod);
var interactions_request_processor_exports = {};
__export(interactions_request_processor_exports, {
  INTERACTIONS_REQUEST_PROCESSOR: () => INTERACTIONS_REQUEST_PROCESSOR,
  InteractionsRequestProcessor: () => InteractionsRequestProcessor
});
module.exports = __toCommonJS(interactions_request_processor_exports);
var import_google_llm = require("../../models/google_llm.js");
var import_llm_agent = require("../llm_agent.js");
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
class InteractionsRequestProcessor {
  // eslint-disable-next-line require-yield
  async *runAsync(invocationContext, llmRequest) {
    const agent = invocationContext.agent;
    if (!agent || !(0, import_llm_agent.isLlmAgent)(agent)) {
      return;
    }
    const model = agent.canonicalModel;
    if ((0, import_google_llm.isGemini)(model) && model.useInteractionsApi) {
      const events = invocationContext.session.events;
      for (let i = events.length - 1; i >= 0; i--) {
        const event = events[i];
        if (event.branch === invocationContext.branch && event.author === agent.name && event.interactionId) {
          llmRequest.previousInteractionId = event.interactionId;
          break;
        }
      }
    }
  }
}
const INTERACTIONS_REQUEST_PROCESSOR = new InteractionsRequestProcessor();
// Annotate the CommonJS export names for ESM import in node:
0 && (module.exports = {
  INTERACTIONS_REQUEST_PROCESSOR,
  InteractionsRequestProcessor
});
