import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
var _a;
import { isBaseAgent } from "../agents/base_agent.js";
const VALID_APP_NAME_RE = /^[a-zA-Z][a-zA-Z0-9_-]*$/;
function validateAppName(name) {
  if (!VALID_APP_NAME_RE.test(name)) {
    throw new Error(
      `Invalid app name '${name}': must start with a letter and can only consist of letters, digits, underscores, and hyphens.`
    );
  }
  if (name === "user") {
    throw new Error("App name cannot be 'user'; reserved for end-user input.");
  }
}
const APP_SIGNATURE_SYMBOL = Symbol.for("google.adk.app");
function isApp(obj) {
  return typeof obj === "object" && obj !== null && APP_SIGNATURE_SYMBOL in obj && obj[APP_SIGNATURE_SYMBOL] === true;
}
_a = APP_SIGNATURE_SYMBOL;
class App {
  constructor(options) {
    this[_a] = true;
    var _a2, _b, _c, _d;
    validateAppName(options.name);
    if (options.rootAgent === void 0 || options.rootAgent === null) {
      throw new Error("rootAgent must be provided.");
    }
    if (!isBaseAgent(options.rootAgent)) {
      throw new TypeError(
        `rootAgent must be a BaseAgent instance, got ${(_c = (_b = (_a2 = options.rootAgent) == null ? void 0 : _a2.constructor) == null ? void 0 : _b.name) != null ? _c : typeof options.rootAgent}`
      );
    }
    this.name = options.name;
    this.rootAgent = options.rootAgent;
    this.plugins = (_d = options.plugins) != null ? _d : [];
    this.resumabilityConfig = options.resumabilityConfig;
  }
}
export {
  App,
  isApp,
  validateAppName
};
