import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
var __defProp = Object.defineProperty;
var __getOwnPropDesc = Object.getOwnPropertyDescriptor;
var __decorateClass = (decorators, target, key, kind) => {
  var result = kind > 1 ? void 0 : kind ? __getOwnPropDesc(target, key) : target;
  for (var i = decorators.length - 1, decorator; i >= 0; i--)
    if (decorator = decorators[i])
      result = (kind ? decorator(target, key, result) : decorator(result)) || result;
  if (kind && result) __defProp(target, key, result);
  return result;
};
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Type } from "@google/genai";
import { experimental } from "../../utils/experimental.js";
import { logger } from "../../utils/logger.js";
import { BaseTool } from "../base_tool.js";
let SearchSkillsTool = class extends BaseTool {
  constructor(toolset) {
    var _a, _b;
    if (!toolset.registry) {
      throw new Error("SearchSkillsTool requires a configured skill registry.");
    }
    super({
      name: "search_skills",
      description: ((_b = (_a = toolset.registry).searchToolDescription) == null ? void 0 : _b.call(_a)) || "Searches for relevant skills in the registry based on a semantic or keyword query."
    });
    this.toolset = toolset;
  }
  _getDeclaration() {
    return {
      name: this.name,
      description: this.description,
      parameters: {
        type: Type.OBJECT,
        properties: {
          query: {
            type: Type.STRING,
            description: "Semantic or keyword search query."
          }
        },
        required: ["query"]
      }
    };
  }
  async runAsync({ args }) {
    const query = args["query"];
    if (!query) {
      return {
        error: "Argument 'query' is required.",
        error_code: "INVALID_ARGUMENTS"
      };
    }
    try {
      const results = await this.toolset.registry.searchSkills(query);
      return results.filter((r) => {
        if (this.toolset.skills[r.name]) {
          logger.warn(
            "Skill naming conflict: skill '".concat(r.name, "' already exists locally. Registry skill is filtered.")
          );
          return false;
        }
        return true;
      });
    } catch (e) {
      return {
        error: "Failed to search skills from registry: ".concat(e.message || e),
        error_code: "REGISTRY_ERROR"
      };
    }
  }
};
SearchSkillsTool = __decorateClass([
  experimental
], SearchSkillsTool);
export {
  SearchSkillsTool
};
