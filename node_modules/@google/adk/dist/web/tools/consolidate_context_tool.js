import {createRequire as topLevelCreateRequire} from 'module';
const require = topLevelCreateRequire(import.meta.url);
/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { Type } from "@google/genai";
import { BaseTool } from "./base_tool.js";
class ConsolidateContextTool extends BaseTool {
  constructor() {
    super({
      name: "consolidate_context",
      description: "Requests context consolidation (compaction) to manage history size. Use this when a subtask is complete and you want to summarize progress and clear detailed history."
    });
  }
  _getDeclaration() {
    return {
      name: this.name,
      description: this.description,
      parameters: {
        type: Type.OBJECT,
        properties: {
          detail: {
            type: Type.STRING,
            description: "Optional description of what has been accomplished so far. This detail will be used to guide the summarization of the history."
          }
        }
      }
    };
  }
  async runAsync({
    args,
    toolContext
  }) {
    toolContext.state.set("temp:consolidate_context", true);
    if (args["detail"] !== void 0) {
      toolContext.state.set(
        "temp:consolidate_context_detail",
        args["detail"]
      );
    }
    return {
      status: "success",
      message: "Context consolidation requested."
    };
  }
}
export {
  ConsolidateContextTool
};
