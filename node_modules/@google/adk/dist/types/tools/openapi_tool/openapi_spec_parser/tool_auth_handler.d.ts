/**
 * @license
 * Copyright 2026 Google LLC
 * SPDX-License-Identifier: Apache-2.0
 */
import { OpenAPIV3 } from 'openapi-types';
import { Context } from '../../../agents/context.js';
import { AuthCredential } from '../../../auth/auth_credential.js';
export interface AuthPreparationResult {
    state: 'pending' | 'done';
    authCredential?: AuthCredential;
}
export declare class ToolAuthHandler {
    private readonly context;
    private readonly authScheme?;
    private readonly authCredential?;
    private readonly credentialKey?;
    constructor(context: Context, authScheme?: OpenAPIV3.SecuritySchemeObject | undefined, authCredential?: AuthCredential | undefined, credentialKey?: string | undefined);
    static fromToolContext(context: Context, authScheme?: OpenAPIV3.SecuritySchemeObject, authCredential?: AuthCredential, options?: {
        credentialKey?: string;
    }): ToolAuthHandler;
    prepareAuthCredentials(): Promise<AuthPreparationResult>;
}
